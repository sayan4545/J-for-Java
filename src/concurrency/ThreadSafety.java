package concurrency;

public class ThreadSafety {
    /*
    Thread safety --> A code block or an object is thread safe if it
    guarantees that if multiople threads tries to access the object or then code block, it doesnt
    result in race condition.
     */
}
